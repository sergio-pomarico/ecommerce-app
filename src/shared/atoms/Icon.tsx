import {createIconSetFromIcoMoon} from 'react-native-vector-icons';
import icoMoonConfig from '@assets/icons/selection.json';

const Icon = createIconSetFromIcoMoon(icoMoonConfig, 'codeo', 'codeo.ttf');

export default Icon;
